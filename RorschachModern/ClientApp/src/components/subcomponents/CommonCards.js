import React from "react";
import { StyledCard } from "./StyledComponents";

export const LoadingCard = () => <StyledCard elevation={5}>Loading.... | Are you winning son?</StyledCard>;

export const ErrorCard = () => <StyledCard elevation={5}>Error | There seems to be a problem son.</StyledCard>;
